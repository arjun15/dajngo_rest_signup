"""
imports configuration of the app
"""

from django.apps import AppConfig  # pragma: no cover


class SignupConfig(AppConfig):  # pragma: no cover
    """
    gives the app's name
    """
    name = 'signup'
